# Codespace Reality Exit Node

پروژه آماده **Sing-box + VLESS-Reality** برای GitHub Codespaces

### نحوه استفاده:
1. دکمه **Code** → **Create codespace on main** را بزنید.
2. بعد از باز شدن Codespace، به صورت خودکار `install.sh` اجرا می‌شود.
3. بعد از اتمام نصب، دستور زیر را بزنید:
   ```bash
   ./start.sh
